import 'package:flutter/material.dart';

class CartDomain extends StatelessWidget {
  const CartDomain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}