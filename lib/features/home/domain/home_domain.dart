import 'package:flutter/material.dart';

class HomeDomain extends StatelessWidget {
  const HomeDomain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}