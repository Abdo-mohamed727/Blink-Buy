import 'package:flutter/material.dart';

class FavouriteData extends StatelessWidget {
  const FavouriteData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}